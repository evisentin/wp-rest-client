/**
 * Apache HttpClient interceptors used by the WordPress client adapter.
 *
 * <p>This package contains request and response interceptors that integrate cross-cutting
 * concerns into the Apache-based client implementation, such as authentication and WordPress-specific error handling.
 *
 * <p>These interceptors are registered internally by the Apache adapter and are not typically
 * used directly by consumers of the client API.
 */
package io.github.evisentin.wordpress.client.adapter.apache.interceptors;
