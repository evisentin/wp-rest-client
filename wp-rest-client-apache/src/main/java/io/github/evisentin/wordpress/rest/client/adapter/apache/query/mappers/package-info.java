/**
 * Query parameter mappers for the Apache WordPress client adapter.
 *
 * <p>This package contains mapper classes that translate domain query objects into
 * Apache HttpClient-compatible query parameters for specific WordPress resources, such as categories, posts, and tags.
 *
 * <p>The mappers encapsulate request parameter construction so that resource-specific
 * filtering and pagination logic remains separate from the main HTTP client implementation.
 */
package io.github.evisentin.wordpress.rest.client.adapter.apache.query.mappers;
