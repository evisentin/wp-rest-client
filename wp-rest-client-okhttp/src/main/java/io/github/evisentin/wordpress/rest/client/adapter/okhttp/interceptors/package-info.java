/**
 * OkHttp interceptors used by the WordPress client adapter.
 *
 * <p>This package contains request and response interceptors that integrate
 * cross-cutting concerns into the OkHttp-based client implementation, such as authentication and WordPress-specific
 * error handling.
 *
 * <p>These interceptors are registered internally by the OkHttp adapter and are
 * not typically used directly by consumers of the client API.
 */
package io.github.evisentin.wordpress.rest.client.adapter.okhttp.interceptors;
